{
  "name": "user-workspace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p www && cp *.html www/ && { [ -f *.css ] && cp *.css www/ || true; } && { [ -f *.js ] && cp *.js www/ || true; } && cp capacitor.config.json www/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "canvas": "^3.1.0",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.0.17"
  }
}
